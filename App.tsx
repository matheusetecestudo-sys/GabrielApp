import React from 'react';
import { Login } from './pages/Login';

const App: React.FC = () => {
  // Temporariamente renderizando apenas o componente Login para depuração
  return <Login />;
};

export default App;